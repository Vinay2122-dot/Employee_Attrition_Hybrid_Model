# Employee_Attrition_Hybrid_Model
Predicts employee attrition using IBM HR data (1,470 records). Compares TabNet, LightGBM, and a Hybrid ensemble. LightGBM achieves 86% accuracy (AUC 0.81). Uses SMOTE, 5-Fold CV, and feature importance. Key drivers: overtime, low salary, age.
